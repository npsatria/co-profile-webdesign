import { initTabs } from './modules/tabs.js';

document.addEventListener('DOMContentLoaded', () => {
    initTabs();
    
    // Inisialisasi library lain di sini nanti (GSAP, Lenis, dll)
    console.log('Atrem Project Profile Initialized');
});
